{
  "LABEL": {
    "name": "Title needs formatting",
    "color": "EEEEEE"
  },
  "CHECKS": {
    "prefixes": ["[Bot] docs: "],
    "regexp": "^(feat|fix|docs|test|ci|cd|task|patch)!?(\\(.*\\))?!?:.*"
  },
  "MESSAGES": {
    "success": "PR title is valid",
    "failure": "PR title is invalid",
    "notice": "PR title needs to pass regex '^(feat|fix|docs|test|ci|cd|task|patch)!?(\\(.*\\))?!?:.*"
  }
}
